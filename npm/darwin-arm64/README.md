# @trevonistrevon/paperbridge-darwin-arm64

macOS ARM64 (Apple Silicon) binary for [paperbridge](https://www.npmjs.com/package/paperbridge).

This package is installed automatically as an optional dependency of `paperbridge`. You should not need to install it directly.

```bash
npm install -g paperbridge
```
