[package]
name = "paperseed-index"
version = "0.8.0"
edition = "2024"
description = "BM25F index for the paperseed local paper corpus"
license = "MIT"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
thiserror = "2"

[dev-dependencies]
tempfile = "3"
